<header>
<h1>{{shop.name}}</h1> 
{{ settings.cover_image | image_url }}
<img src="{{ settings.cover_image | image_url }}" width="200px" height="auto">
<ul>
  {% for link in linklists.main-menu.links %}
    <li>
      <a href="{{ link.url }}">{{ link.title }}</a>
    </li>
  {% endfor %}
  </ul>
  
  </header>
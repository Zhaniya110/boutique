<div >
    <p>{{ product.title }}</p>
   <img src="{{ product.image | image_url }}" width="200px" height="auto">
    <p>{{ product.price | money }}</p>
    <a href="{{ product.url }}">See product </a>
    </div>
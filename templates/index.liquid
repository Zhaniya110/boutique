{% comment %} 
<div class="wrapper">
    {% for product in collections.all.products %}
        <div class="product">
          <h2>{{ product.title }}</h2>
          <p>{{ product.price | money }}</p>
          <img src="{{ product | image_url }}" width="200px" height="auto">
          <a href="{{ product.url }}">View Product</a>
        </div>
      {% endfor %}
     </div>
 {% endcomment %}
<h2>{{ collections['вечерние-платья'].title }} </h2>

 {% for product in collections['вечерние-платья'].products %}

<div class="max-w-sm rounded overflow-hidden shadow-lg"> 
  <img class="w-full" src="{{ product | image_url }}" width="200px" height="auto">

  <div class="px-6 py-4">  <h3>{{ product.title }} </h3>
 <p>{{ product.price | money }} </p>
</div>
 
</div>
 {{- product.title | link_to: product.url }}
{% endfor %}  
<div class="wrapper">

    {% assign myString = 'Boutique. Theme is under development...'  %}

    <p>   {{ myString }}</p> 

   {% capture myString2 %}
    Please Wait.
    {% endcapture %}

    <p>{{ myString2 }}</p> 

    {% assign status == 0 %}
    {% assign string = "DressCode" %}
    {%  if status == 1 or string contains "DressCode"%}
        you can view the page_description
    {% elsif status == 2 %}
        contact admin
        {% else %}
        restricted area
        {% endif %}
    </div>